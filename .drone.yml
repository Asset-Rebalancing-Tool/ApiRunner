---
kind: pipeline
type: docker
name: default

steps:
  - name: build
    image: gradle:jdk17
    environment:
      MAVENUSER:
        from_secret: th_nexus_user
      MAVENPASSWORD:
        from_secret: th_nexus_password
      JAVA_HOME: /opt/java/openjdk
    commands:
      - gradle publish
